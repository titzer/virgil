def X_ = Aeneas.startup = run;
def run() {
	UnitTests.fatal = CLOptions.FATAL.val;
	var result = UnitTests.run([]);
	if (result == 0) {
		Terminal.put("Unit tests ");
		Terminal.green("passed", ());
		Terminal.put(".\n");
	}
}
