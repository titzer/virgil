//@execute 0=5; 1=5; 2=5; 3=5; 4=5; 5=5
def classify<T>(a: T) -> int {
	var d = double.!(a);
	return 5;
}
def inputs = [
	double.infinity,
	double.nan,
	0.0d,
	0.5d,
	1d,
	1.0000000001d
];
def main(a: int) -> int {
	return classify(inputs[a]);
}
