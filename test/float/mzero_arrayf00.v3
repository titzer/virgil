//@execute 0=-2147483648; 1=-2147483648
// A run-time store of -0.0 into a freshly allocated array must not be elided.
def main(a: int) -> int {
	var arr = Array<float>.new(2);
	arr[a] = -0f;
	return int.view(arr[a]);
}
