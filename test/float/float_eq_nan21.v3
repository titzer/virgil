//@execute 0=3; 1=3; 2=0; 3=8; 4=16
// double_eq_nan21 for float.
def a = [
	((), 0f),
	((), -0f),
	((), float.nan),
	((), float.infinity),
	((), 1f)
];
def main(i: int) -> int {
	var l = a[i];
	var out = 0;
	for (j < a.length) {
		if (l == a[j]) out |= (1 << u5.view(j));
	}
	return out;
}
