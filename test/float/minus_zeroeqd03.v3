//@execute 0=true; 1=true
class D(x: double) { }
def vals = [D.new(0d), D.new(-0d)];

def eq<T>(x: T, y: T) -> bool {
	return x == y;
}

def main(a: int) -> bool {
	return eq(vals[0].x, vals[a].x);
}
