//@execute 0=!TypeCheckException; 1=!NullCheckException
class D(x: int) describes A {}
class S {}
class A extends S descriptor D {} // dead

def ss = [S.new(), null];

def main(x: int) -> int {
	return A.!(ss[x]).descriptor.x;
}