//@execute 0=7; -7=-7; 10=27
class D describes A {}
class A(x: int, y: int) descriptor D {}

def d = D.new();

def main(x: int) -> int {
	var t = (d, x + 3, x + 4);
	var a = A.new(t);
	return a.x + a.y;
}
