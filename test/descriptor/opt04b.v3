//@execute 0=true; 1=true
class D(x: int) describes A { }
class A descriptor D { }

def ds = [D.new(3), D.new(5)];

def main(a: int) -> bool {
	def as = [A.new(ds[0]), A.new(ds[1])];
	return as[a].descriptor.x > 0; // constant-fold true
}
