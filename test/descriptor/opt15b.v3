//@execute 0=true; -888=true
class S { var xx: i48; }
class D(x: int) extends S describes A {
	new() { xx += x; }
}
class A extends S descriptor D {
	new() { xx += 44; }
}

def D_new = D.new;

def main(x: int) -> bool {
	def a = A.new(D_new(x));
	return a.descriptor.x == a.descriptor.x;
}
