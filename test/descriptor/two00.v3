//@execute = true
class DD describes D {}
class D descriptor DD describes A {}
class A descriptor D {}

def dd = DD.new();
def d = D.new(dd);
def a = A.new(d);

def main() -> bool {
	return dd == d.descriptor && d == a.descriptor;
}
