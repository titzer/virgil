//@execute 0=0; 3=3
class DL<T> describes List<T> {}
class List<T>(head: T, tail: List<T>) descriptor DL<T> {}
def main(max: int) -> int {
	var list: List<int>;
	for (i = 0; i < max; i++) {
		list = List.new(DL<int>.new(), i, list);
	}
	for (l = list; l != null; l = l.tail) {
	}
	return max;
}
