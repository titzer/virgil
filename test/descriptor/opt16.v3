//@execute 0=44; -888=-844
class S { var xx: i48; }
class D(x: int) extends S describes A {
	new() { xx += x; }
}
class A extends S descriptor D {
	new() { xx += 44; }
}

def main(x: int) -> int {
	def a = A.new(D.new(x));
	return int.view(a.xx + a.descriptor.xx);
}
