//@seman
class DA describes A { }
class DB extends DA describes B { }
class A descriptor DA { }
class B extends A descriptor DB { }

def a = A.new(DA.new());
def b = B.new(DB.new());

def as = [a, b];

def main(a: int) -> bool {
	return DB.?(as[a].descriptor);
}