//@execute 0=-12; 1=-12
class D(x: int) describes A {}
class S {}
class A extends S descriptor D {} // dead

def ss = [S.new(), null];

def main(x: int) -> int {
	var o = ss[x];
	if (A.?(o)) return A.!(o).descriptor.x;
	return -12;
}