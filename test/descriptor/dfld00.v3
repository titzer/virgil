//@execute 0=false; 1=true; 2=true
class D(x: int) describes A { }
class A descriptor D { }

def d1 = D.new(33);
def d2 = D.new(44);

def as = [A.new(d1), A.new(d2), A.new(d2)];

def main(a: int) -> bool {
	return as[a].descriptor.x == d2.x;
}
