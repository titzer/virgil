//@execute 0=3; 1=1
class D(t: (int, bool, int)) describes A { }
class A descriptor D { }
def a = A.new(D.new((1, true, 2)));

def main(x: int) -> int {
	match (x) {
		0 => return a.descriptor.t.0 + a.descriptor.t.2;
		1 => return if(a.descriptor.t.1, 1, 0);
	}
	return -1;
}
