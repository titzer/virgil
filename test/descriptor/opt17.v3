//@execute 0=true; 1=true
class S { var xx: int; }
class D(x: int) extends S describes A {
	new() { this.xx = u20.view(x); }
}
class A descriptor D { }

def ds = [D.new(3), D.new(5)];

def main(a: int) -> bool {
	def o = A.new(ds[a]);
	return o.descriptor.xx > 0; // constant-fold true
}
