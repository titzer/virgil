//@execute = 5
// Contrast: the generic class holds the descriptor itself, with no superclass. Must pass.
class Desc<T>(n: int) describes A<T> { }
class A<T>(item: T) descriptor Desc<T> { }
def main() -> int {
	var a = A<int>.new(Desc<int>.new(5), 0);
	return a.descriptor.n;
}
