//@execute 0=7; -7=-7; 10=27
class D<T> describes A<T> {}
class A<T>(x: T) descriptor D<T> {}

def d = D<(int, int)>.new();

def test<T>(d: D<T>, v: T) -> T {
	var t = (d, v);
	var a = A.new(t);
	return a.x;
}

def main(x: int) -> int {
	var t = test(d, (x + 3, x + 4));
	return t.0 + t.1;
}
