//@execute = false
class D describes A {} // descriptor class and object are not reachable
class A descriptor D {}

def a1 = A.new(D.new());
def a2 = A.new(D.new());

def main() -> bool {
	return a1 == a2;
}
