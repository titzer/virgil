//@execute 0=true; 1=true
class D(x: int) describes A { }
class A descriptor D { }

def ds = [D.new(3), D.new(5)];

def main(a: int) -> bool {
	def o = A.new(ds[a]);
	return o.descriptor.x > 0; // constant-fold true
}
