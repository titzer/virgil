//@execute 0=0; 5=10

def main(l:int) -> int {

	var ai32 = Array<i32>.new(l);
	for (i < l) {
		ai32[i] = i32.view(i);
	}
	var si32: i32 = 0i32;
	for (i < l) {
		si32 += ai32[i];
	}
	return int.!(si32);
}
