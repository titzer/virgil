# Locals must be described with their declared types and with locations that
# cover registers, stack slots, and values that move between them.
info scope main
info scope check
info scope spin
ptype enum Color
