# Class references are pointers to structures with the declared fields; array
# references are pointers to a structure with a length and inline elements.
ptype struct Point
ptype struct Line
info scope main
info scope total
ptype main::a
ptype main::s
