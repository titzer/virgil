//@execute 0=42; 1=43; 2=44
// {CiTest.forceGc()} must compile on every target, and is simply a nop on
// targets without a runtime garbage collector.
class A(x: int) { }
def main(a: int) -> int {
	var v = A.new(42 + a);
	CiTest.forceGc();
	return v.x;
}
