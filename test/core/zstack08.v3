//@execute 44=32
def m(a: int) {
	while (true) {
		if (a > 0) break;
		if (a > 0) break;
	}
	var x = a + 1;
	var y = a + 1;
	var z = y + 2;
	if (z < 0) {
		call2(x, y, call(z));
	} else {
		call2(x, y, call(z));
	}
}
def call(x: int) -> int { return x; }
def call2(x: int, y: int, z: int) { }
def main(a: int) -> int {
	m(a);
	return 32;
}
