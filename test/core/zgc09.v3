//@execute 0=0; 1=1; 4=4
class zgc09(x: int) { }
def main(a: int) -> int {
	var t = (zgc09.new(11), zgc09.new(12));
	for (i = 0; i < a; i++) {
		var n = check(t);
		var m = check(t);
		t = n;
	}
	return a;
}
def check(a: zgc09, b: zgc09) -> (zgc09, zgc09) {
	if (a.x + 1 != b.x) {
		var y = 1 / 0;
	}
	return (b, zgc09.new(b.x + 1));
}
