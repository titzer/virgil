//@execute 0=73
type Shape {
	case Circle(r: int);
	case Square(s: int);
}
def Shape.area() -> int {
	match (this) {
		Circle(r) => return r * r * 3;
		Square(s) => return s * s;
	}
}
def main(a: int) -> int { return Shape.Circle(4).area() + Shape.Square(5).area(); }
