//@execute 0=34; 22=100
class A(var y: int) {
	new() { g++; }
}
class B extends A {
	var init: bool;
	new(y: int) super(y + 33 + g) { this.init = true; }
}
var g: int;

def main(x: int) -> int {
	g = x;
	def a = B.new(x);
	return if(a.init, a.y + g);
}
