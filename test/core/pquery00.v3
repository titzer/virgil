//@execute 0=-42; 1=-42
class Some<T>(x: T) {
	def is<U>(val: U) -> bool {
		return Some<U>.?(this) && Some<U>.!(this).x == val;
	}
}

def main(a: int) -> int {
	var x = Some.new(a == a);
	if (x.is(a)) return 11;
	return -42;
}
