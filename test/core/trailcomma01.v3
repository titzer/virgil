//@execute 0=3; 1=6; 2=1
// a trailing comma does not change tuple arity or element order
def main(i: int) -> int {
	var t = (1, 2,);
	var u = (1, 2, 3,);
	var v = (1,);
	match (i) {
		0 => return t.0 + t.1;
		1 => return u.0 + u.1 + u.2;
		_ => return v;
	}
}
