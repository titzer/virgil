//@execute 0=1; 5=16
def main(a: int) -> int {
	var x: Array<int>;
	var sum = 0;

	x = [a];
	for (i < a) sum += x.length;

	x = [a];
	for (i < a) sum += x.length;

	x = [a];
	for (i < a) sum += x.length;

	return sum + x.length;
}
