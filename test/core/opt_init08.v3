//@execute 0=33; 1=34; -5=28
class A {
	var x: int;
}

// dead store followed by live store: first store should be eliminated
def main(a: int) -> int {
	var o = A.new();
	o.x = 0;       // dead store
	o.x = a + 33;  // live store
	return o.x;
}
