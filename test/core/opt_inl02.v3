//@execute 0=3; 5=7; 99=-2
def find(a: int) -> int {
	var t = a * 2 + 1;
	for (i < 10) {
		var s = i * i + i;
		if (s > t) return i;
		if (s == t) return 0 - i;
	}
	return -1;
}
def top(a: int) -> int #inline-into {
	return find(a) + find(a + 1);
}
def main(a: int) -> int {
	return top(a);
}
