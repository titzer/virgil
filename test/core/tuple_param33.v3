//@execute 0=45; -99=-54
component tuple_param33 {
	def main(a: int) -> int {
		return apply(m, (a, 1, 2, 3, 4, 5, 6, 7, 8, 9));
	}
	def apply<T>(f: T -> int, p: T) -> int {
		return f(p);
	}
	def m(a: int, b: int, c: int, d: int, e: int, f: int, g: int, h: int, i: int, j: int) -> int {
		return a + b + c + d + e + f + g + h + i + j;
	}
}
