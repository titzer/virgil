//@execute 0=33; 22=55
class A(var y: int) {
	var z = y;
	new() {
		z += 3;
	}
}
class B extends A {
	var init: bool;
	new(y: int) super(y + 30) { this.init = true; }
}

def main(x: int) -> int {
	def a = B.new(x);
	return if(a.init, a.z);
}
