//@execute 0=false
class C<T> {
	def m<U>(a: T) -> U;
}
def main(arg: int) -> bool {
	var a: int -> int = C<int>.new().m;
	var b: bool -> int = C<bool>.new().m;
	return cmp(a, b);
}
def cmp<A, B>(a: A, b: B) -> bool {
	return (B.?(a) && B.!(a) == b) || (A.?(b) && A.!(b) == a);
}
