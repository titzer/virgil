//@execute 0=65535; 1=33825
def main(a: int) -> int {
	var vals = [
		(a, a),
		(a, -a),
		(-a, a),
		(-a, -a)
	];

	var res = 0;
	for (i < vals.length) {
		for (j < vals.length) {
			if (vals[i] == vals[j]) {
				res |= 1 << u5.!((i*4) + j);
			}
		}
	}
	return res;
}
