//@execute 0=4; 1=-1; 2=4
def parse_array(a: bool) -> int {
	if (a) return -1;
	while (cond(a)) {
		if (ok) return -2;
	}
	if (a) return -3;
	return 4;
}
def cond(a: bool) -> bool {
	return false;
}

var ok: bool;
def main(a: int) -> int {
	ok = a > 0;
	return parse_array(a == 1);
}
