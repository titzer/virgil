//@execute 0=1; 1=2; 2=3; 3=4
// Type method ref through multi-level hierarchy with virtual dispatch
type A {
    case _;
    def kind() -> int { return 1; }
}
type A.B {
    case P;
    case _;
    def kind() -> int { return 2; }
}
type A.B.C {
    case Q;
    def kind() -> int { return 3; }
}
type A.D {
    case R;
    def kind() -> int { return 4; }
}
def main(arg: int) -> int {
    var a: A;
    if (arg == 1) a = A.B.P;
    if (arg == 2) a = A.B.C.Q;
    if (arg == 3) a = A.D.R;
    var f = A.kind;
    return f(a);
}
