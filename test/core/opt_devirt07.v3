//@execute 0=13; 1=!NullCheckException
class A {
	def m() -> int { return 13; }
}
class B extends A {
	def m() -> int { return 23; }
}

def apply(f: void -> int) -> int {
	return f();
}

def main(a: int) -> int {
	var o = [A.new(), null][a];
	return apply(o.m);
}
