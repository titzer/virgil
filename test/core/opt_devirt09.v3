//@execute 0=13; 1=!NullCheckException
class A {
	def m() -> int { return 13; }
}
class B extends A {
}

def apply(f: void -> int) -> int {
	return f();
}

def main(a: int) -> int {
	var o = [B.new(), null][a];
	return apply(o.m);
}
