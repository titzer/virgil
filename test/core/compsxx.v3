//@execute (0,0)=0; (1,2)=3
component A {
	var x: int = 17;
	def setx(i: int) { x = i; }
}

component B {
	var x: int;
	def setx(i: int) { x = i; }
}

def main(ax: int, bx: int) -> int {
	A.setx(ax);
	B.setx(bx);
	return A.x + B.x;
}
