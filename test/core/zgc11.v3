//@execute 1=1
class zgc11(x: int) { }
def main(a: int) -> int {
	var t = (zgc11.new(11), zgc11.new(12));
	for (i = 0; i < a; i++) {
		t = check(t);
	}
	return a;
}
def check(a: zgc11, b: zgc11) -> (zgc11, zgc11) {
	if (a.x + 1 != b.x) div(0);
	return (b, zgc11.new(b.x + 1));
}
def div(a: int) -> int {
	return 1 / a;
}