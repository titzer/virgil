//@execute 0=0; 1=1; 4=4
class zgc06(x: int) { }
def main(a: int) -> int {
	var t = (zgc06.new(11), 11, zgc06.new(12));
	for (i = 0; i < a; i++) {
		t = check(t);
	}
	return a;
}
def check(a: zgc06, x: int, b: zgc06) -> (zgc06, int, zgc06) {
	if (a.x + 1 != b.x) {
		var y = 1 / 0;
	}
	return (b, x + 1, zgc06.new(b.x + 1));
}
