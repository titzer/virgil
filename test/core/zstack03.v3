//@execute 0=17
def main(a: int) -> int {
	var x = a + 1;
	var y = a + 2;
	var z = call1(x);
	var w = call2(x, y, z);
	return w;
}
def call1(x: int) -> int {
	return x + 6;
}
def call2(x: int, y: int, z: int) -> int {
	return x + y + z + 7;
}
