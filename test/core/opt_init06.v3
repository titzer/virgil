//@execute 0=0; 5=10; -3=9
class A(x: int, y: int) {}

// conditional read: only one field used per branch; both should forward
def main(a: int) -> int {
	var o = A.new(a, a * -3);
	if (a < 0) return o.y;
	return o.x * 2;
}
