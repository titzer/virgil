//@execute false=0; true=40
var g: int;
def add(x: int) { g += x; }
def main(a: bool) -> int {
	g = 0;
	var t = if(a, (true, true, true, true), (false, false, false, false));
	if (t.0) add(10);
	if (t.1) add(10);
	if (t.2) add(10);
	if (t.3) add(10);
	return g;
}
