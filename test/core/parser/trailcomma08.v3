//@parse
// trailing comma in variant case parameters and match patterns
type V {
	case A(x: int, y: int,);
	case B;
}
def f(v: V) -> int {
	match (v) {
		A(x, y,) => return x + y;
		B => return 0;
	}
}
