//@execute 0=0; 1=0; 88230489=33809015; -9834898=-5826550
def main(a: int) -> int {
	return garble(a, -1);
}
def garble(x: int, y: int) -> int {
	return (x >> u5.view(y)) ^ (x >> u4.view(y)) ^ (x >> u3.view(y)) ^ (x >> u2.view(y)) ^ (x >> u1.view(y));
}