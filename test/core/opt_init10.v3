//@execute 0=5; 1=6; -1=4
class A {
	var x: int;
}

// store to one object should not kill forwarded load from different object
def main(a: int) -> int {
	var p = A.new();
	var q = A.new();
	p.x = 5;
	q.x = a;
	return p.x + q.x;
}
