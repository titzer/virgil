//@execute 0=0; 3=21; -2=-14
class A {
	var x: int;
	var y: int;
	new(v: int) {
		x = v * 2;
		y = v * 5;
	}
}

// two fields set in constructor body; both loads should forward
def main(a: int) -> int {
	var o = A.new(a);
	return o.x + o.y;
}
