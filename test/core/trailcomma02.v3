//@execute 0=3; 1=30
// trailing commas in parameter lists and call argument lists
def f(a: int, b: int,) -> int { return a + b; }
class C(x: int, y: int,) { }
def main(i: int) -> int {
	if (i == 0) return f(1, 2,);
	var c = C.new(10, 20,);
	return c.x + c.y;
}
