//@execute 3=5; 7=5
def main(a: int) -> int {
	var sum = 0;
	var hello = copy(a);
	for (i < hello.length) {
		sum++;
		hello = copy(a);
	}
	return sum;
}
def copy(a: int) -> Array<int> {
	return [9, a, 9, 9, 9];
}
