//@execute =true

// Test depends on TagUtils.v3

def main() -> bool {
    def c1 = GCRefTestC.new(1, 2);
    def p1 = Pointer.atObject(c1);

    def c2 = GCRefTestC.new(2, 3);
    def p2 = Pointer.atObject(c2);

    return CiRuntime.isOop(p1) && CiRuntime.isOop(p2);
}
