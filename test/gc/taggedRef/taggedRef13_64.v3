//@execute 4=4; 5=5; 262143=262143; 131070=131070 
// Test depends on TagUtils.v3

// Testing the upper tag 
// 262143 is 2^18 - 1 (18 tag bits are filled)
// 131070 is 2^17 - 2 (16 tag bits are filled, leftmost and rightmost empty)
def main(a: int) -> int {
	def c1 = GCRefTestC.new(1, 2);
	def c1Tagged = TagUtils.setAuxTag<GCRefTestC>(c1, a);
	
	// induce GC
	RiGc.forceGC();

	def tag = TagUtils.getAuxTag(c1Tagged);

	// Test tag is correct after GC updates ref
	return tag;
}

