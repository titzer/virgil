//@execute 0=0; 1=11; 2=30; 5=135; 100=40700
class B(y: int) { }
class A(b: B, x: int) { }

def main(a: int) -> int {
	var sum = 0;
	for (i < a) {
		var b1 = B.new(1 + a), b2 = B.new(2 + a);
		var o = A.new(b1, 3 + a);
		CiTest.forceGc();
		sum += b1.y + b2.y + o.x + o.b.y;
	}
	return sum;
}
