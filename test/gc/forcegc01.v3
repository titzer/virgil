//@execute 0=42; 1=43; 2=44
class A(x: int) { }

def main(a: int) -> int {
	var v = A.new(42 + a);
	var before = GcStats.gc_count;
	CiTest.forceGc();
	if (GcStats.gc_count != before + 1) return -1; // no collection happened
	return v.x;
}
