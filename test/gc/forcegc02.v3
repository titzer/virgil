//@execute 0=7; 1=11; 2=15
class B(y: int) { }
class A(b: B, x: int) { }

def main(a: int) -> int {
	var b1 = B.new(1 + a), b2 = B.new(2 + a);
	var o = A.new(b1, 3 + a);
	CiTest.forceGc();
	return b1.y + b2.y + o.x + o.b.y;
}
