def foo(a: int) -> Array<byte> {
	return Array<byte>.new(a);
}
def main(args: Array<string>) -> int {
	for (i < args.length + 1) {
        	var x = foo(1);
                System.puts("allocated: ");
                System.puti(x.length);
                System.ln();
        }
        return 0;
}
