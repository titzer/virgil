//@execute 0=true; 1=false; 2=false
type vt_cmp08a { case M; case N; case O; }
def all = [vt_cmp08a.M, vt_cmp08a.N, vt_cmp08a.O];
def eq<T>(a: T, b: T) -> bool {
	return a == b;
}
def main(a: int) -> bool {
	return eq(all(a), vt_cmp08a.M);
}
