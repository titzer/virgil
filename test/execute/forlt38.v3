//@execute = 31
def main() -> int {
	var sum = 0;
	def matrix = [
		[0, 1],
		[0, 3],
		[4, 5],
		[5, 6, 7]
	];
	for (i < matrix.length) {
		for (j < matrix(i).length) sum = sum + matrix(i)(j);
	}
	return sum;
}
