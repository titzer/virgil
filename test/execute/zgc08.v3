//@execute 0=0; 1=1; 4=4
class zgc08(x: int) { }
def main(a: int) -> int {
	var t = (zgc08.new(11), 11, zgc08.new(12));
	for (i = 0; i < a; i++) {
		var n = check(t);
		var m = check(t);
		t = n;
	}
	return a;
}
def check(a: zgc08, x: int, b: zgc08) -> (zgc08, int, zgc08) {
	if (a.x + 1 != b.x) {
		var y = 1 / 0;
	}
	return (b, x + 1, zgc08.new(b.x + 1));
}
