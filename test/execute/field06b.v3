//@execute 0='*'; 1='h'; 2='e'; 3='*'

class field06b_obj {
    var a: byte = 'h';
    var b: byte = 'e';
}

component field06b {
    var foo: field06b_obj = field06b_obj.new();

    def main(arg: int) -> byte {
	if (arg == 1) return foo.a;
	if (arg == 2) return foo.b;
	return '*';
    }
}
