//@execute 0=11
def main(a: int) -> int {
	var x = a + 1;
	var y = a + 2;
	if (a < 0) {
		var z = call1(x);
		var w = call2(x, y, z);
	}
	return 11;
}
def call1(x: int) -> int {
	return x + 6;
}
def call2(x: int, y: int, z: int) -> int {
	return x + y + z + 7;
}
