//@execute 0=28; 1=29; 2=30
component tuple_param10 {
	def main(arg: int) -> int {
		var t = (1, 2, 3, 4, 5, 6, arg + 7);
		return f(t);		
	}
	def f(a: (int, int, int, int, int, int, int)) -> int {
		return a.0 + a.1 + a.2 + a.3 + a.4 + a.5 + a.6;
	}
}
