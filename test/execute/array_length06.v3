//@execute 0 = 3
component array_length06 {
	var array: Array<int> = [0, 1, 2];
	def main(a: int) -> int {
		var f = Array<int>.length;
		return do(f);
	}
	def do(f: Array<int> -> int) -> int {
		return f(array);
	}
}
