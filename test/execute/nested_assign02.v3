//@execute 0=4; 1=5; 2=6
component nested_assign02 {
	def main(a: int) -> int {
		return m(a, a = 0, a, a = 1, a, a = 2);
	}
	def m(a: int, b: int, c: int, d: int, e: int, f: int) -> int {
		return a + b + c + d + e + f;
	}
}
