//@execute = true
type vt_cmp20a<H> { case A; case B; case C; case D; case E; }
def test<I>() -> bool {
	def x = [vt_cmp20a<I>.A, vt_cmp20a<I>.B, vt_cmp20a<I>.C, vt_cmp20a<I>.D, vt_cmp20a<I>.E];
	def y = [vt_cmp20a<I>.A, vt_cmp20a<I>.B, vt_cmp20a<I>.C, vt_cmp20a<I>.D, vt_cmp20a<I>.E];
	for (i = 0; i < x.length; i++) {
		if (x(i) != y(i)) return false;
	}
	return true;
}
def main() -> bool {
	return test<byte>() && test<string>();
}