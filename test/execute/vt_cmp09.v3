//@execute 0=true; 1=false; 2=false
type vt_cmp09a { case M; case N; case O; }
def all = [vt_cmp09a.M, vt_cmp09a.N, vt_cmp09a.O];
def eq<T>() -> (T, T) -> bool {
	return T.==;
}
def main(a: int) -> bool {
	return eq<vt_cmp09a>()(all(a), vt_cmp09a.M);
}
