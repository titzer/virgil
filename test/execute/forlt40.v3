//@execute 3=39; 7=43
def main(a: int) -> int {
	var sum = 0;
	var hello = copy(a);
	for (i < hello.length) {
		sum = sum + hello(i);
		hello = copy(a);
	}
	return sum;
}
def copy(a: int) -> Array<int> {
	return [9, a, 9, 9, 9];
}
