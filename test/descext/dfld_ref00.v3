//@execute = 100
class Meta(id: int) { }
class D(m: Meta) describes C { }
class C descriptor D { }
def meta = Meta.new(555);
def d = D.new(meta);
def main() -> int {
	CiRuntime.setDescriptorExtension(d, 0, 2 * Pointer.SIZE);
	if (d.m != meta) return 1;
	return if(d.m.id == 555, 100, 2);
}
