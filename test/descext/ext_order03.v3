//@execute = 100
class D(n: int) describes A { }
class A descriptor D { }
def d = D.new(9);
var a: A;

def main() -> int {
	a = A.new(d);
	CiRuntime.setDescriptorExtension(d, 0, 1 * Pointer.SIZE); // branded last
	return if(a.descriptor.n == 9, 100, 1);
}
