//@execute = 100
class D describes C { }
class C(x: int) descriptor D { }
def d = D.new();
def main() -> int {
	CiRuntime.setDescriptorExtension(d, 0, 8 * Pointer.SIZE);
	var c = C.new(d, 42);
	return if(c.x == 42, 100, c.x);
}
