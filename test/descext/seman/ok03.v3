//@seman
class D<T> describes C<T> { }
class C<T> descriptor D<T> { }

def di = D<int>.new();
def db = D<bool>.new();

def main() {
	CiRuntime.setDescriptorExtension(di, 0, 16);
	CiRuntime.setDescriptorExtension<D<bool>>(db, 0, 16);
}
