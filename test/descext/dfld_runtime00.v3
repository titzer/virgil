//@execute = 100
class D(tag: int) describes C { }
class C descriptor D { }
def main() -> int {
	var d = D.new(321); // descriptor allocated at runtime (heap)
	CiRuntime.setDescriptorExtension(d, 0, 2 * Pointer.SIZE);
	var c = C.new(d);
	return if(d.tag == 321, 100, d.tag);
}
