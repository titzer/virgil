//@execute = 12
enum E { A, B, C }
var calls = 0;
def get() -> E.set {
	calls++;
	return E.A | E.C;
}
def main() -> int {
	// the iterated set expression is evaluated exactly once
	var count = 0;
	for (e in get()) count++;
	return calls * 10 + count;
}
