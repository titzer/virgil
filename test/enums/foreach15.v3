//@execute 0=12; 1=134; 2=1234
enum E { A, B, C, D }
def all = E.A | E.B | E.C | E.D;
def main(a: int) -> int {
	var digits = 0;
	for (e in all) {
		if (a == 0 && e == E.C) break;
		if (a == 1 && e == E.B) continue;
		digits = digits * 10 + e.tag + 1;
	}
	return digits;
}
