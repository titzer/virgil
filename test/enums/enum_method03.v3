//@execute 0=10; 1=20; 2=30
// Enum method with parameters
enum E { A, B, C; def mul(x: int) -> int { return (tag + 1) * x; } }
def main(i: int) -> int {
    match (i) {
        0 => return E.A.mul(10);
        1 => return E.B.mul(10);
        2 => return E.C.mul(10);
    }
    return -1;
}
