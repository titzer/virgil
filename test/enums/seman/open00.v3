//@seman
// Open enum: match requires default arm
enum E { A, B, _ }
component C {
    def f(e: E) -> int {
        match (e) {
            A => return 1;
            B => return 2;
            _ => return 0;
        }
    }
}
