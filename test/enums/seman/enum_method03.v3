//@seman
// Private enum method not accessible externally, but callable internally
enum E { A, B;
    def pub() -> int { return priv(); }
    private def priv() -> int { return 1; }
}
def main() {
    var x: int = E.A.pub();
}
