//@seman
// Subtype enum: match on closed subtype-typed variable is exhaustive without _ arm
enum E1 { A, B, _ }
enum E1.More { C, D }
component C {
    def f(e: E1.More) -> int {
        match (e) {
            C => return 1;
            D => return 2;
        }
    }
}
