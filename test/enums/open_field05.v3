//@execute 0=1; 1=10; 2=20
// Access field on root-typed variable holding subtype value
enum E(x: int) { A(1), _ }
enum E.S1(x: int) { C(10), D(20) }
def main(i: int) -> int {
    var cases: Array<E> = [E.A, E.S1.C, E.S1.D];
    if (i < 0 || i >= cases.length) return -1;
    return cases[i].x;
}
