//@execute 0=0; 1=1; 2=2
// Enum method using tag field
enum E { A, B, C; def id() -> int { return tag; } }
def main(i: int) -> int {
    match (i) {
        0 => return E.A.id();
        1 => return E.B.id();
        2 => return E.C.id();
    }
    return -1;
}
