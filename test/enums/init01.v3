//@execute 0=1; 1=1; 2=!BoundsCheckException
def es = [C.new(), C.new()];
enum E(typename: string) {
	A("a"),
	B("bb")
}
class C {
	def e: E;
	def typename = e.typename;
}

def main(a: int) -> int {
	return es[a].typename.length;
}
