//@execute 0=1; 1=17; 2=23
// Form 3: implicit — subtype has no param decl, inherits parent's
enum E(x: int) { A(1), B(17), _ }
enum E.S3 { H(23) }
def main(i: int) -> int {
    var cases: Array<E> = [E.A, E.B, E.S3.H];
    if (i < 0 || i >= cases.length) return -1;
    return cases[i].x;
}
