//@execute = 129
enum E { A, B, C }
def main() -> int {
	// iterate the full set twice
	var sum = 0;
	for (e in E.set.all) sum = sum * 10 + e.tag + 1;
	for (e in E.set.all) sum += e.tag + 1;
	return sum;
}
