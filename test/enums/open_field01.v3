//@execute 0=1; 1=17; 2=15
// Form 2: (super) keyword — subtype cases provide field values
enum E(x: int) { A(1), B(17), _ }
enum E.S2(super) { D(15) }
def main(i: int) -> int {
    var cases: Array<E> = [E.A, E.B, E.S2.D];
    if (i < 0 || i >= cases.length) return -1;
    return cases[i].x;
}
