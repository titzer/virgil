//@seman = AnnotationError
// A grouping constant is read from the component field's initializer.
@target(TargetKinds.Container) @type onContainer;
class C { @onContainer def m() { } }
component M { def main(a: int) -> int { return 0; } }
