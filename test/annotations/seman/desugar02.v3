//@seman
// Annotation methods become variant methods and are type-checked.
@type withMethods(value: int) {
	def ok() -> bool { return value != 0; }
	private def helper() -> int { return value + 1; }
}
component M { def main(a: int) -> int { return 0; } }
