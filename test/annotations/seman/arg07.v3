//@seman = AnnotationError
// An alias supplies its own arguments, so a use may not add more.
@type t(v: int);
@def al = @t(1);
@al(2) class C { }
component M { def main(a: int) -> int { return 0; } }
