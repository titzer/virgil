//@seman
// A name may be declared twice for disjoint targets, and each use picks the right one.
@target(TargetKind.Class) @type dual;
@target(TargetKind.Enum) @type dual(v: int);
@dual class C { }
@dual(1) enum E { X }
component M { def main(a: int) -> int { return 0; } }
