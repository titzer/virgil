//@seman
// The rewrite reaches nested positions, not just a bare top-level name.
@type nested(a: Array<Annotation>, b: (Annotation, int), c: Annotation -> int);
component M { def main(a: int) -> int { return 0; } }
