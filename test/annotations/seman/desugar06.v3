//@seman
// Even where a user has their own "Annotation", an annotation field still means the root.
class Annotation { var x: int; }
@type holder(value: Annotation);
component M {
	var mine: Annotation;
	def main(a: int) -> int { return 0; }
}
