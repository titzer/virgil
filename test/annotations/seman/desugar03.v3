//@seman = TypeError
// A method body on an annotation is really type-checked: "value" is an int, not a string.
@type bad(value: int) {
	def oops() -> string { return value; }
}
component M { def main(a: int) -> int { return 0; } }
