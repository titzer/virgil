//@seman = AnnotationError
// A field defined in terms of itself is reported, not looped on.
@type t(v: int);
component H { def a: int = H.b; def b: int = H.a; }
@t(H.a) class A { }
component M { def main(a: int) -> int { return 0; } }
