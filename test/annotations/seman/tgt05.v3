//@seman = AnnotationDeclError
// Overloads must be disjoint.
@target(TargetKind.Class) @type dual;
@target(TargetKind.Class | TargetKind.Enum) @type dual;
component M { def main(a: int) -> int { return 0; } }
