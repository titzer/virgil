//@seman
// Subtype names carry '@' and are unforgeable, so declaring "@type cold" does not create
// a user-visible type named "cold".
@type cold;
class cold { }
component M { def main(a: int) -> int { return 0; } }
