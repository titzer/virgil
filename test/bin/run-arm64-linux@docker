#!/usr/bin/env bash
OUT=$1
shift
TEST=$1
shift

BASE=$(basename $TEST .v3)
exec docker run --rm \
  --platform linux/arm64 \
  -v "$OUT:$OUT" \
  -v "$PWD:$PWD" \
  --workdir "$PWD" \
  alpine:3.20 \
  "$OUT/$BASE" "$@"
