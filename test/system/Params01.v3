// tests that params work
component Params01 {
	def main(a: Array<string>) {
		for (i = 0; i < a.length; i++) {
			if (i > 0 ) System.puts(" ");
			System.puti(i);
			System.puts(":");
			System.puts(a(i));
		}
	}
}
