//@execute = true
component stat03 {
	def stat: Array<int> = Array.new(32);
	def main() -> bool {
		var path = Pointer.atContents("xxx.txt\x00");
		return Linux.stat(path, Pointer.atContents(stat)) < 0;
	}
}
