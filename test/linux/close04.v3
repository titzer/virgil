//@execute = true
component close04 {
	def main() -> bool {
		var path = Pointer.atContents("test.txt\x00");
		var fd = Linux.open(path, 0, 0);
		return Linux.close(fd) == 0 && Linux.close(fd) < 0;
	}
}
