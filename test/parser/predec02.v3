//@parse
class X {
	def x() {
		--x;
		if (--x) --x;
		else --x;
		if (--x) { --x; }
		else { --x; }

		while (--x) --x;
		while (--x) { --x; }

		match (--x) {}
		match (--x) {} else { --x; }

		for (i = --x; --x < --x; --x) {}
		for (i = --x; --x < --x; (--x, --x)) { --x; }
	}
}
