//@parse
component foreach01 {
	def m() {
		for (x in e) ;
		for (x in e.f) ;
		for (x in e.f()) ;
		for (x in (e, f)) ;
		for (x in e + f) ;
		for (x in T.?(e)) ;
		for (x in T.!(e)) ;
		for (x in e == f) ;
		for (x in if(e, a)) ;
		for (x in if(e, a, b)) ;
		for (x in (y = e)) ;
		for (x in y = e) ;
	}
}
