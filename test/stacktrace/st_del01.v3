component st_del01 {
	def main(args: Array<string>) -> int {
		var a = args.length;
		var f = if(a == 1, del1, del2);
		return f(a);
	}
	def del1(a: int) -> int {
		return a / (a - 1);
	}
	def del2(a: int) -> int {
		return a / (a - 2);
	}
}
