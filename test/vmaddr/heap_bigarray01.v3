//@execute 0=!HeapOverflow
def use<T>(x: T) -> T #no-inline { return x; }

def main(a: int) -> int {
	var n = 268435455 + a; // one more than the largest allocatable Array<long>
	return use(Array<long>.new(n)).length;
}
