//@seman

// A feature-detection program for Virgil named tuple elements.
// You can copy this file into your project and use it to programmatically detect
// that the compiler supports named tuple elements for your project and target platform.

def f() -> (x: int, y: string) {
	return (0, "foo");
}

def TestRet() {
	var t = f();
	var x1: int = t.x;
	var x2: string = t.y;
}
