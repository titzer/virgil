//@execute 0=true; 1=true
// The other half of IEEE equality.
type T {
	case F(x: float);
	case I(x: int);
}
def POS: Array<T> = [T.F(0f), T.F(-0f)];
def NEG: Array<T> = [T.F(-0f), T.F(0f)];
def main(a: int) -> bool {
	return POS[a] == NEG[a];
}
