//@execute 0=false; 1=false; 2=true
// The same cached boxed value, loaded twice.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan), T.F(float.infinity)];
def main(a: int) -> bool {
	var l = CACHE[a], r = CACHE[a];
	return l == r;
}
