//@execute 0=false
// A constant index into a constant array.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan)];
def main(a: int) -> bool {
	var l = CACHE[0], r = CACHE[0];
	return l == r;
}
