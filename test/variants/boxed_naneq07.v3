//@execute 0=false; 1=false
// Only the second of two float fields is NaN.
type T {
	case F(x: float, y: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(1f, float.nan), T.F(1f, float.nan)];
def main(a: int) -> bool {
	var v = CACHE[a];
	return v == v;
}
