//@execute 0=0; 1=2; 2=4
type T {
	case A(x: int);
	case B(x: int); // dead
	case C(x: int);
	case D(x: int); // dead
	case E(x: int);
}

var vs = [T.A(11), T.C(22), T.E(33)];

def main(a: int) -> int {
	return vs[a].tag;
}
