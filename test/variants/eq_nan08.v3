//@execute = 0
type F {
	case M(x: float);
}
def NANS: Array<float> = [float.nan];
def main() -> int {
	var k = float.nan;
	var v = NANS[0];
	if (int.view(k) != int.view(v)) return -1;
	var folded = if(F.M(k) == F.M(k), 1, 0);
	var live = if(F.M(v) == F.M(v), 1, 0);
	return folded * 10 + live;
}
