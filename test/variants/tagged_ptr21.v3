//@execute 0=13; 1=7; 2=25
type T #unboxed #packed {
	case A(x: string, y: u8);
	case B(x: u16);
}

def ts = [T.A("abc", 1), T.B(7), T.A("defgh", 2)];

def main(a: int) -> int {
	match (ts[a]) {
		A(str, y) => return str.length + y * 10;
		B(v) => return v;
	}
}
