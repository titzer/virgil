//@execute 0=0; 3=9; -2=-6
type Inner(x: int, y: int) {}
type Outer(inner: Inner, z: int) {}

// nested ADT: field access through two levels
def main(a: int) -> int {
	var o = Outer(Inner(a, a * 2), a * 3);
	return o.inner.x + o.inner.y;
}
