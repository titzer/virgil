//@execute = !NullCheckException
type HH {
	case One(a: int);
}

class A { def m() -> HH { return HH.One(1); } }
class B extends A { def m() -> HH { var x = 1/0; return HH.One(2); } }

def oa = A.new();
def ob: B;
def oc = B.new();

var a: u1;
var b: u1;
var c: u1;
def x = Array<HH>.new(1);

def main() -> bool {
	a = A.m(oa).tag;
	b = A.m(ob).tag;
	c = A.m(oc).tag;
	return false;
}
