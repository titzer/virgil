//@execute 0=false; 1=false
// boxed_naneq00 for a double payload.
type T {
	case D(x: double);
	case L(x: long);
}
def CACHE: Array<T> = [T.D(double.nan), T.D(double.nan)];
def main(a: int) -> bool {
	var l = CACHE[a], r = CACHE[a];
	return l == r;
}
