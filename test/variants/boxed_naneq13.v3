//@execute 0=true; 1=true
// Control for the fastpath.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.I(7), T.I(-7)];
def main(a: int) -> bool {
	var v = CACHE[a];
	return v == v;
}
