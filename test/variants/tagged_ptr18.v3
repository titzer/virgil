//@execute 0=3; 1=-7; 2=42; 3=5; 4=-7
type T #unboxed #packed {
	case A(x: string);
	case N;
	case B(x: u8);
}

def ts = [T.A("abc"), T.N, T.B(42), T.A("defgh"), T.N];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		N => return -7;
		B(v) => return v;
	}
}
