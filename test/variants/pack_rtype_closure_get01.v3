//@execute 0=8; 1=3
type T #unboxed {
	case F(f: int -> int);
	case S(s: string);
}
def inc(x: int) -> int { return x + 1; }
def fs = [inc];
def ss = ["abc"];
def main(a: int) -> int {
	var v = if(a == 0, T.F(fs[0]), T.S(ss[0]));
	match (v) {
		F(f) => return f(7);
		S(s) => return s.length;
	}
}
