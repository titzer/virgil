//@execute 22=300; 11=289
type X {
	case M;
	case N;
}
def m<T>(a: T) -> int {
	match (a) {
		x: int	=> return x - 11;
		x: X	=> return 22;
	}
	return 300 - 55;
}

def main(a: int) -> int {
	return m<int>(a) + m<X>(X.M) + m<X>(X.N) + m<bool>(true);
}
