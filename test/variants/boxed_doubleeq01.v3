//@execute 0=1; 1=2; 2=4; 3=0
type T {
	case I(v: int);
	case F(v: double);
}

def ts = [T.I(33), T.I(-33), T.F(33.333d), T.F(double.nan)];

def main(a: int) -> int {
	var l = ts[a];
	var out = 0;
	for (i < ts.length) {
		var r = ts[i];
		if (l == r) out |= (1 << u5.!(i));
	}
	return out;
}
