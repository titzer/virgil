//@execute 0=false; 1=false
// The unboxed counterpart of boxed_naneq01.
type T #unboxed {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def main(a: int) -> bool {
	var v = CACHE[a];
	return v == v;
}
