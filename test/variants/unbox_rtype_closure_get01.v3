//@execute 0=1; 1=2; 2=4; 3=8
type T #unboxed {
	case F(f: int -> int);
	case S(s: string);
}
def vs = [
	T.F(fun x => x + 1),
	T.F(fun x => x - 1),
	T.S("abc"),
	T.S(null)
];
def main(a: int) -> int {
	var d = vs[a], r = 0;
	for (i < vs.length) if (d == vs[i]) r |= 1 << u5.view(i);
	return r;
}
