//@execute 0=3; 1=333; 2=5; 3=4444; 4=!NullCheckException
type T #unboxed #packed {
	case A(x: string);
	case B(x: u31);
}

def ts = [T.A("abc"), T.B(333), T.A("defgh"), T.B(4444), T.A(null)];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		B(v) => return v;
	}
}
