//@execute 0=true; 1=true
// The negated form.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def main(a: int) -> bool {
	var v = CACHE[a];
	return v != v;
}
