//@execute 0=7; 1=3; 2=250; 3=5; 4=!NullCheckException
type T #unboxed #packed {
	case B(x: u8);
	case A(x: string);
}

def ts = [T.B(7), T.A("abc"), T.B(250), T.A("defgh"), T.A(null)];

def main(a: int) -> int {
	match (ts[a]) {
		B(v) => return v;
		A(str) => return str.length;
	}
}
