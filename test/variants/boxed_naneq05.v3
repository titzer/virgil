//@execute 0=false; 1=false
// Reached through a generic equality.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def eq<X>(x: X) -> bool {
	return x == x;
}
def main(a: int) -> bool {
	return eq(CACHE[a]);
}
