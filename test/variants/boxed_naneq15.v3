//@execute =false
// Both operands are compile-time constants, so this is decided entirely by the constant folder.
type T {
	case F(x: float);
	case I(x: int);
}
def main() -> bool {
	return T.F(float.nan) == T.F(float.nan);
}
