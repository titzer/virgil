//@execute 0=-8; 1=-16; 2=-20; 3=-22
type T #unboxed #packed {
	case A(x: i6, y: i6);
	case B(x: i6, y: i6);
}

def vs = [T.A(-5, -3), T.A(-9, -7), T.B(-11, -9), T.B(-12, -10)];

def main(a: int) -> int {
	match (vs[a]) {
		A(x, y) => return x + y;
		B(x, y) => return x + y;
	}
}
