//@execute 0=0; 1=255; 2=-1; 3=-2
type T #unboxed {
	case Byte(b: byte);
	case Bool(b: bool);
}

def ts = [T.Byte(0), T.Byte(255), T.Bool(true), T.Bool(false)];

def main(a: int) -> int {
	match (ts[a]) {
		Byte(b) => return b;
		Bool(b) => return if(b, -1, -2);
	}
}