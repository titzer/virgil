//@execute =!NullCheckException
type HH {
	case One(a: int);
}
class C(x: HH) { }

def c = C.new(HH.One(0));

def foo(c: C) -> HH {
	return c.x;
}
def main() -> bool { 
	return foo(null).tag == foo(c).tag;
}
