//@execute 0=0; 1=2; 2=4; 3=0
// boxed_enumeq00's full matrix, with two distinct never-equal cases.
type T {
	case F(x: double);
	case E(y: float);
	case G;
	case H;
}
def ts: Array<T> = [T.F(double.nan), T.G, T.H, T.E(float.nan)];
def main(i: int) -> int {
	var l = ts[i];
	var out = 0;
	for (j < ts.length) {
		if (l == ts[j]) out |= (1 << u5.view(j));
	}
	return out;
}
