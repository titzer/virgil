//@execute 0=false; 1=false
// One local compared against itself.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def main(a: int) -> bool {
	var l = CACHE[a];
	return l == l;
}
