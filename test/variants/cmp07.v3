//@execute 0=true; 1=false; 2=false
type cmp07a { case M; case N; case O; }
def all = [cmp07a.M, cmp07a.N, cmp07a.O];
def eq<T>(a: T, b: T) -> bool {
	return a == b;
}
def main(a: int) -> bool {
	return eq(all(a), cmp07a.M);
}
