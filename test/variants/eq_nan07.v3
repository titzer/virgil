//@execute 0=false; 1=false; 2=true
type F #unboxed {
	case M(x: float);
	case N;
}
def NANS: Array<float> = [float.nan, float.nan, 1f];
def main(a: int) -> bool {
	var x = NANS[a];			// runtime value, not a constant
	return F.M(x) == F.M(x);
}
