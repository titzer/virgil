//@execute 0=1; 1=2; -5=-5
type A(x: int, y: int) {}

// conditional field read: both fields should forward to init values
def main(a: int) -> int {
	var o = A(a, a + 1);
	return if(a < 0, o.x, o.y);
}
