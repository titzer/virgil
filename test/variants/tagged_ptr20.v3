//@execute 0=3; 1=1073741823; 2=5; 3=1
type T #unboxed #packed {
	case A(x: string);
	case B(x: u30);
}

def ts = [T.A("abc"), T.B(0x3FFFFFFF), T.A("defgh"), T.B(1)];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		B(v) => return v;
	}
}
