//@execute 0=3; 1=0; 2=3
def main(a: int) -> int {
	def p = i5.pack<(u2,bool,u2)>((0b11,false,0b11));
	def u: (u2,bool,u2) = i5.unpack<(u2,bool,u2)>(p);
	match (a) {
		0 => return u.0;
		1 => return if(u.1, 1, 0); 
		2 => return u.2; 
		_ => return 42;
	}

}
