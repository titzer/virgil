//@execute 0=false; 1=false
// The same cached boxed value, loaded twice.
type T #unboxed {
	case F(nonce: int, x: double);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(11, double.nan), T.F(22, double.nan)];
def main(a: int) -> bool {
	var l = CACHE[a], r = CACHE[a];
	return l == r;
}
