//@execute 0=-9; 1=-7; 2=-5; 3=-3
type T {
	case A(x: i4, y: i3);
	case B(x: i4, y: i3);
}

def vs = [T.A(-5, -4), T.A(-4, -3), T.B(-3, -2), T.B(-2, -1)];

def main(a: int) -> int {
	match (vs[a]) {
		A(x, y) => return int.view(x) + int.view(y);
		B(x, y) => return int.view(x) + int.view(y);
	}
}
