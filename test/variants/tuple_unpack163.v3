//@execute 0=1; 1=3
def main(a: int) -> int {
	def p = u5.pack<(u1,u2)>((0b1,0b11));
	def u: (u1,u2) = u5.unpack<(u1,u2)>(p);
	match (a) {
		0 => return u.0; 
		1 => return u.1; 
		_ => return 42; 
	}
}
