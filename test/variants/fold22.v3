//@execute = 11
type T {
	case A(x: int);
	case B(x: int);
}
var global = 11; // will be inferred to be constant
def dead() {
	global = 12; // unreachable
}

def main() -> int {
	var ts = [T.A(global), T.B(global)]; // will be foldable after reachability
	return T.B.!(ts[1]).x;
}
