//@execute 0=3; 1=1; 2=2; 3=0; 4=5
enum E { X, Y, Z }
type T #unboxed #packed {
	case A(x: string);
	case B(e: E);
}

def ts = [T.A("abc"), T.B(E.Y), T.B(E.Z), T.B(E.X), T.A("defgh")];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		B(e) => return e.tag;
	}
}
