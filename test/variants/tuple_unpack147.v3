//@execute 0=3; 1=3; 2=0
def main(a: int) -> int {
	def p = u5.pack<(u2,u2,bool)>((0b11,0b11,false));
	def u: (u2,u2,bool) = u5.unpack<(u2,u2,bool)>(p);
	match (a) {
		0 => return u.0;
		1 => return u.1; 
		2 => return if(u.2, 1, 0); 
		_ => return 42;
	}
}
