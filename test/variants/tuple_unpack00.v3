//@execute 0=0; 1=3; 2=3
def main(a: int) -> int {
	def p = i5.pack<(bool,u2,u2)>((false,0b11,0b11));
	def u: (bool,u2,u2) = i5.unpack<(bool,u2,u2)>(p);
	match (a) {
		0 => return if(u.0, 1, 0);
		1 => return u.1; 
		2 => return u.2; 
		_ => return 42;
	}
}
