//@execute =true
// ub_zeroeq00 decided entirely by the constant folder.
type T #unboxed {
	case F(x: float);
	case D(x: double);
}
def main() -> bool {
	return T.F(0f) == T.F(-0f) && T.D(-0d) == T.D(0d);
}
