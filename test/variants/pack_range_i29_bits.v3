//@execute 0=-1147402773; 1=-141464620; 2=-1899399254; 3=610531861
type T #unboxed #packed {
	case R(s: string);
	case I(x: i29);
}
def cell: Array<T> = [T.R("r"), T.R(null)];
def main(a: int) -> int {
	var acc = 0, r = cell[0];
	for (b < 29) {
		var p = 1L << u6.view(b);
		if (a == 1) p = ~p;
		if (a == 2) p = p - 1;
		if (a == 3) p = 0 - p;
		var x = i29.view(p);
		cell[1] = T.I(x);
		if (cell[1] == r) return -100;
		match (cell[1]) {
			R(s) => return -200;
			I(y) => {
				if (y != x) return -1 - b;
				acc = acc * 31 + int.view(y);
			}
		}
	}
	return acc;
}
