//@execute 0=3; 1=133; 2=5; 3=244; 4=!NullCheckException
type T #unboxed #packed {
	case A(x: string);
	case B(x: u8);
}

def ts = [T.A("abc"), T.B(133), T.A("defgh"), T.B(244), T.A(null)];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		B(v) => return v;
	}
}
