//@execute 1=!TypeCheckException; 0=100
type T {
	case A(x: int);
	case B(x: float);
}

def main(a: int) -> int {
	def v: T = if(false, T.B(1.0f), T.A(100));
	if (a == 0) return int.!(T.A.!(v).x);
	if (a == 1) return int.!(T.B.!(v).x);
	return -42;
}
