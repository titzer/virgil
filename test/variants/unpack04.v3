//@execute 1234=true
def main(a: int) -> bool {
    def u32Res: int = u32.unpack<int>(u32.pack<int>(a));
    def u33Res: int = u33.unpack<int>(u33.pack<int>(a));
    def u34Res: int = u34.unpack<int>(u34.pack<int>(a));
    def u35Res: int = u35.unpack<int>(u35.pack<int>(a));
    def u36Res: int = u36.unpack<int>(u36.pack<int>(a));
    def u37Res: int = u37.unpack<int>(u37.pack<int>(a));
    def u38Res: int = u38.unpack<int>(u38.pack<int>(a));
    def u39Res: int = u39.unpack<int>(u39.pack<int>(a));
    def u40Res: int = u40.unpack<int>(u40.pack<int>(a));
    def u41Res: int = u41.unpack<int>(u41.pack<int>(a));
    def u42Res: int = u42.unpack<int>(u42.pack<int>(a));
    def u43Res: int = u43.unpack<int>(u43.pack<int>(a));
    def u44Res: int = u44.unpack<int>(u44.pack<int>(a));
    def u45Res: int = u45.unpack<int>(u45.pack<int>(a));
    def u46Res: int = u46.unpack<int>(u46.pack<int>(a));
    def u47Res: int = u47.unpack<int>(u47.pack<int>(a));
    def u48Res: int = u48.unpack<int>(u48.pack<int>(a));
    def u49Res: int = u49.unpack<int>(u49.pack<int>(a));
    def u50Res: int = u50.unpack<int>(u50.pack<int>(a));
    def u51Res: int = u51.unpack<int>(u51.pack<int>(a));
    def u52Res: int = u52.unpack<int>(u52.pack<int>(a));
    def u53Res: int = u53.unpack<int>(u53.pack<int>(a));
    def u54Res: int = u54.unpack<int>(u54.pack<int>(a));
    def u55Res: int = u55.unpack<int>(u55.pack<int>(a));
    def u56Res: int = u56.unpack<int>(u56.pack<int>(a));
    def u57Res: int = u57.unpack<int>(u57.pack<int>(a));
    def u58Res: int = u58.unpack<int>(u58.pack<int>(a));
    def u59Res: int = u59.unpack<int>(u59.pack<int>(a));
    def u60Res: int = u60.unpack<int>(u60.pack<int>(a));
    def u61Res: int = u61.unpack<int>(u61.pack<int>(a));
    def u62Res: int = u62.unpack<int>(u62.pack<int>(a));
    def u63Res: int = u63.unpack<int>(u63.pack<int>(a));
    def u64Res: int = u64.unpack<int>(u64.pack<int>(a));

    if (u32Res != a) { def x = 1/0; }
    if (u33Res != a) { def x = 1/0; }
    if (u34Res != a) { def x = 1/0; }
    if (u35Res != a) { def x = 1/0; }
    if (u36Res != a) { def x = 1/0; }
    if (u37Res != a) { def x = 1/0; }
    if (u38Res != a) { def x = 1/0; }
    if (u39Res != a) { def x = 1/0; }
    if (u40Res != a) { def x = 1/0; }
    if (u41Res != a) { def x = 1/0; }
    if (u42Res != a) { def x = 1/0; }
    if (u43Res != a) { def x = 1/0; }
    if (u44Res != a) { def x = 1/0; }
    if (u45Res != a) { def x = 1/0; }
    if (u46Res != a) { def x = 1/0; }
    if (u47Res != a) { def x = 1/0; }
    if (u48Res != a) { def x = 1/0; }
    if (u49Res != a) { def x = 1/0; }
    if (u50Res != a) { def x = 1/0; }
    if (u51Res != a) { def x = 1/0; }
    if (u52Res != a) { def x = 1/0; }
    if (u53Res != a) { def x = 1/0; }
    if (u54Res != a) { def x = 1/0; }
    if (u55Res != a) { def x = 1/0; }
    if (u56Res != a) { def x = 1/0; }
    if (u57Res != a) { def x = 1/0; }
    if (u58Res != a) { def x = 1/0; }
    if (u59Res != a) { def x = 1/0; }
    if (u60Res != a) { def x = 1/0; }
    if (u61Res != a) { def x = 1/0; }
    if (u62Res != a) { def x = 1/0; }
    if (u63Res != a) { def x = 1/0; }
    if (u64Res != a) { def x = 1/0; }
    return true;
}