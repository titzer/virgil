//@execute 0=false; 1=false
// Variant equality compares a float field with IEEE `==`.
type F {
	case M(x: float);
}
def NANS: Array<float> = [float.nan, float.nan];
def main(a: int) -> bool {
	var x = NANS[a];			// runtime value, not a constant
	return F.M(x) == F.M(x);
}
