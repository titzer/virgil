//@execute 0=1; 1=1; 2=1
enum E { A, B, C }
def main(a: int) -> int {
	def p = i5.pack<(u1,u1,E)>((0b1,0b1,E.A));
	def u: (u1,u1,E) = i5.unpack<(u1,u1,E)>(p);
	match (a) {
		0 => return u.0;
		1 => return u.1; 
		2 => return if(u.2==E.A, 1, 0);
		_ => return 42;
	}
}
