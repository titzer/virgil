//@execute 0=0; 1=1; 2=2; 3=0; 4=0; 7=0
// a bit pattern that is not a valid tag maps to tag 0, the same rule that
// reading an enum field of a layout uses
enum E { A, B, C }
def f(v: u3) -> E { return u3.unpack<E>(v); }
def main(a: int) -> int { return f(u3.view(a)).tag; }
