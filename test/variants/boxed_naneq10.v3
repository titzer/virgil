//@execute 0=false; 1=false
// Two distinct objects holding identical NaN bits.
type T {
	case F(x: float);
	case I(x: int);
}
def NANS: Array<float> = [float.nan, float.nan];
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def main(a: int) -> bool {
	return CACHE[a] == T.F(NANS[a]);
}
