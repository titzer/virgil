//@execute 0=true; 1=true; 2=true; 3=true
// Control for the fastpath.
type T {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(1.5f), T.F(-0f), T.F(float.infinity), T.F(-float.infinity)];
def main(a: int) -> bool {
	var v = CACHE[a];
	return v == v;
}
