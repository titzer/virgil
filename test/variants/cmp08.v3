//@execute 0=true; 1=false; 2=false
type cmp08a { case M; case N; case O; }
def all = [cmp08a.M, cmp08a.N, cmp08a.O];
def eq<T>(a: T, b: T) -> bool {
	return a == b;
}
def main(a: int) -> bool {
	return eq(all(a), cmp08a.M);
}
