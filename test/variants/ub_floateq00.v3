//@execute 0=1; 1=2; 2=4; 3=8
type T #unboxed {
	case I(v: int);
	case F(v: float);
}

def ts = [T.I(33), T.I(-33), T.F(33.333f), T.F(-33.333f)];

def main(a: int) -> int {
	var l = ts[a];
	var out = 0;
	for (i < ts.length) {
		var r = ts[i];
		if (l == r) out |= (1 << u5.!(i));
	}
	return out;
}
