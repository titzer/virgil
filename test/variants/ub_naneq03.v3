//@execute =false
// The unboxed comparison decided entirely by the constant folder.
type T #unboxed {
	case F(x: float);
	case I(x: int);
}
def main() -> bool {
	return T.F(float.nan) == T.F(float.nan);
}
