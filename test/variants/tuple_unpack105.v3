//@execute 0=33; 1=65; 2=1
def main(a: int) -> int {
	def p = i45.pack<(u6,u7,float)>((0b100001,0b1000001,1F));
	def u: (u6,u7,float) = i45.unpack<(u6,u7,float)>(p);
	match (a) {
		0 => return u.0; 
		1 => return u.1; 
		2 => return if(u.2==1F, 1, 0); 
		_ => return 42;
	}
}
