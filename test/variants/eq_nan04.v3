//@execute 0=false; 1=false
// eq_nan03 for an #unboxed variant.
type F #unboxed {
	case M(x: double);
	case N;
}
def NANS: Array<double> = [double.nan, double.nan];
def main(a: int) -> bool {
	var x = NANS[a];			// runtime value, not a constant
	return F.M(x) == F.M(x);
}
