//@execute 0=-1430532899; 1=1090051375
type T #unboxed {
	case I(val: u32);
	case F(val: float);
}

def ts = [T.I(0xAABBCCDD), T.F(7.777f)];

def main(a: int) -> u32 {
	var v = ts[a & 1];
	match (v) {
		I(v) => return v;
		F(v) => return u32.view(v);
	}
}
