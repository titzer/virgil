//@execute =!NullCheckException
type HH {
	case One(a: int);
}
class C(x: HH) { }

def c = C.new(HH.One(0));

def foo(c: C) -> u1 {
	return c.x.tag;
}
def main() -> bool { 
	return foo(null) == foo(c);
}
