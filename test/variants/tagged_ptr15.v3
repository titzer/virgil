//@execute 0=3; 1=1; 2=5; 3=0
type T #unboxed #packed {
	case A(x: string);
	case B(b: bool);
}

def ts = [T.A("abc"), T.B(true), T.A("defgh"), T.B(false)];

def main(a: int) -> int {
	match (ts[a]) {
		A(str) => return str.length;
		B(b) => return if(b, 1, 0);
	}
}
