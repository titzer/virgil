//@execute 0=-5; 1=-9; 2=-11; 3=-12
type T #unboxed {
	case A(x: i5);
	case B(x: i5);
}

def vs = [T.A(-5), T.A(-9), T.B(-11), T.B(-12)];

def main(a: int) -> int {
	match (vs[a]) {
		A(x) => return x;
		B(x) => return x;
	}
}
