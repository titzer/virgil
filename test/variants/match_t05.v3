//@execute 11=300
type X {
	case M;
	case N;
}
def m<T>(a: T) -> int {
	match (a) {
		x: int	=> return x;
		x: X.M	=> return 22;
		x: X.N	=> return 22;
	}
	return 300 - 55;
}

def main(a: int) -> int {
	return m<int>(a) + m<X>(X.M) + m<X>(X.N) + m<bool>(true);
}
