//@execute 0=1; 1=33; 2=65
def main(a: int) -> int {
	def p = u45.pack<(float,u6,u7)>((1F,0b100001,0b1000001));
	def u: (float,u6,u7) = u45.unpack<(float,u6,u7)>(p);
	match (a) {
		0 => return if(u.0==1F, 1, 0); 
		1 => return u.1; 
		2 => return u.2; 
		_ => return 42; 
	}
}
