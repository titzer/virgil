//@execute 0=false; 1=false
// The unboxed comparison reached through the `==` method reference.
type T #unboxed {
	case F(x: float);
	case I(x: int);
}
def CACHE: Array<T> = [T.F(float.nan), T.F(float.nan)];
def main(a: int) -> bool {
	var v = CACHE[a];
	var f = T.==;
	return f(v, v);
}
