//@execute 0=false; 1=false
// The same cached boxed value, loaded twice.
type T {
	case F(x: float);
	case I(x: int);
}
def n(x: float) -> T {
	return T.F(x);
}
def CACHE: Array<T> = [n(float.nan), n(float.nan)];
def main(a: int) -> bool {
	var l = CACHE[a], r = CACHE[a];
	return l == r;
}
