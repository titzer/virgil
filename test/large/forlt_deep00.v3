//@execute 0=512
def check(a: int, b: int, c: int, d: int, e: int, f: int, g: int, h: int, i: int) {
	var v = 
		(a << 8) |
		(b << 7) |
		(c << 6) |
		(d << 5) |
		(e << 4) |
		(f << 3) |
		(g << 2) |
		(h << 1) |
		(i << 0);
	if (v != count++) {
		def z = 1 / 0;
	}
}
var count = 0;
def main(a: int) -> int {
	count = 0;
	var l = 0;
	for (a < 2)
	for (b < 2)
	for (c < 2)
	for (d < 2)
	for (e < 2)
	for (f < 2)
	for (g < 2)
	for (h < 2)
	for (i < 2) {
		check(a, b, c, d, e, f, g, h, i);
		l++;
	}
	return l;
}