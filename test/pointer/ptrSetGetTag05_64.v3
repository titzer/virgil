//@execute =true
def p() -> Pointer {
    return Pointer.NULL + -1L;
}
def main() -> bool {
    def ptr = CiRuntime.setAuxTag(p(), 0);
    def mask = (ptr - Pointer.NULL) ^ -1L;
    return mask == long.view(CiRuntime.AUX_TAG_MASK);
}