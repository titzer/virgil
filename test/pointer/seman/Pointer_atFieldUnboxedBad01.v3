//@seman = TypeError @ 10:33
// Disallow pointer to *boxed* field of a component

type V(x: int);
component C {
	var v = V(13);
}

def main() -> int {
	var p = Pointer.atField(C.v.x);
	p.store<int>(17);
	return C.v.x;
}
