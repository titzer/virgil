//@seman = TypeError @ 14:33
// Disallow pointer to *boxed* field of a class

type V(x: int);
class C {
	var v: V;
	new(i: int) {
		v = V(i);
	}
}

def main() -> int {
	var c = C.new(13);
	var p = Pointer.atField(c.v.x);
	p.store<int>(17);
	return c.v.x;
}
