//@execute = false
class Object { }

def a = Object.new(), b = Object.new();
var f: AnyRef;

def main() -> bool {
	var ptr_f = Pointer.atField(f);
	ptr_f.store<Object>(a);
	return ptr_f.load<Object>() == b;
}
