//@execute 0=-7; 1=-5; 2=-3; 3=-1
def main(a: int) -> long {
    // 32 bits
    def mask = 0b111111111111111111111111111111001;
    var ptr = Pointer.NULL + mask;
    ptr = CiRuntime.setAuxTag(ptr, a);
    return long.view(ptr - Pointer.NULL);
}