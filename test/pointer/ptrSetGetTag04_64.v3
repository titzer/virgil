//@execute 0=0; 1=1; 2=2; 3=3
def main(a: int) -> long {
    // 64 bits
    def mask = 0b1111111111111111111111111111111111111111111111111111111111111111;
    var ptr = Pointer.NULL + mask;
    ptr = CiRuntime.setAuxTag(ptr, a);
    return long.view(CiRuntime.getAuxTag(ptr));
}