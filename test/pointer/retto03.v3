//@execute 0=0; 44=44; -44=-44
def foo(a: int) -> int {
	var sp = CiRuntime.callerSp() + (0 - Pointer.SIZE);
	CiRuntime.returnTo<int>(sp, a);
	return -1;
}
def main(a: int) -> int {
	return foo(a);
}
