//@execute =1
class C {
    def a: int;
    def b: int;
    new(a, b) {}
}
def main() -> int {
    def c = C.new(1, 2);
    var p = Pointer.atObject(c);
    
    p = CiRuntime.setAuxTag(p, 1);
    return int.view(CiRuntime.getAuxTag(p));
}
