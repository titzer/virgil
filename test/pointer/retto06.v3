//@execute = 4242
def foo() -> double {
	var sp = CiRuntime.callerSp() + (0 - Pointer.SIZE);
	CiRuntime.returnTo<double>(sp, 42.42d);
	return -1;
}
def main() -> int {
	return int.!(foo() * 1000d);
}
