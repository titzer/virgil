//@execute =true
def p() -> Pointer {
    return Pointer.NULL + -1;
}
def main() -> bool {
    def ptr = CiRuntime.setAuxTag(p(), 0);
    def mask = (ptr - Pointer.NULL) ^ -1;
    return mask == int.view(CiRuntime.AUX_TAG_MASK);
}