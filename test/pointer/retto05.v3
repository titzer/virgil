//@execute 0=11; 44=55; -44=-33
def foo(a: int) -> int {
	var sp = CiRuntime.callerSp() + (0 - Pointer.SIZE);
	CiRuntime.returnTo<int>(sp, a + 11);
	return -1;
}
def main(a: int) -> int {
	return foo(a);
}
