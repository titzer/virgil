//@seman
// a case type denotes exactly one case, so it needs no tag and can be packed
type T { case A(x: u4, y: u4); case B(z: u8); }
type W(a: u4, c: T.A) { }
def a = u8.pack<T.A>;
def b = u8.unpack<T.A>;
def c = u12.pack<(u4, T.A)>;
def d = u12.pack<W>;
