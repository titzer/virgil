//@execute = 42
def r = test();
def test() -> int {
	for (i < 16) {
		for (j < 16) {
			var p = u32.pack((0x3u4, i4.view(i), i4.view(j), 0x88u8));
			if (p > 0x3FF88) return -11;
			if (p < 0x30088) return -12;
		}
	}
	return 21;
}
def main() -> int {
	return test() + r;
}