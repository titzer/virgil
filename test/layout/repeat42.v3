//@execute 0=1; 1=1; 2=1; 3=1
layout R {
	+0	x:	u2[4];
	=4;
}

// A store is a full-byte store, so it clears the poked upper bits.
def main(a: int) -> byte {
	var data = Array<byte>.new(4);
	var ref = Ref<R>.at(data, 0);
	ref.x[a] = 3;
	data[a] |= 0xFC;
	ref.x[a] = 1;
	return data[a];
}
