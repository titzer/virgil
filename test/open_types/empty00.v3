//@execute = 0
type B {
	case A;
	case _;
}
type X extends B {
	case C;
	case D;
}

def bs: Array<B> = [];

def sum(bs: Array<B>) -> int {
	var sum = 0;
	for (b in bs) {
		sum += b.tag;
	}
	return sum;
}

def main() -> int {
	return sum(bs);
}
