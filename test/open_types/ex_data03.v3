//@execute -1=77; 1=88
type T {
	case A;
	case _;
}
type U(x: int) extends T {
}

def u = U(77), v = U(88);

def main(a: int) -> int {
	var x: T = if(a < 0, u, v);
	match (x) {
		A => return -11;
		m: U => return m.x;
		_ => return -12;
	}
}
