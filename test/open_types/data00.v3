//@execute 0=1; 1=1; 2=2; 3=2
type Base {
	case _;
}
type One(x: int) extends Base {}
type Two(x: int, y: int) extends Base {}

def bs = [One(4), One(5), Two(4, 5), Two(5, 6)];

def main(a: int) -> int {
	match (bs[a]) {
		x: One => return 1;
		x: Two => return 2;
		_ => return -11;
	}
}
