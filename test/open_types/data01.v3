//@execute 0=32; 1=33; 2=20
type Base {
	def m() -> int;

	case _;
}

type One(x: int) extends Base {
	def m() => x;
}
type Two(x: int, y: int) extends Base {
	def m() => x + y;
}

def bs = [Two(10, 22), Two(30, 3), One(20)];

def main(a: int) => bs[a].m();
