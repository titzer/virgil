//@execute = 30
// Shorthand form: type Tree.Branch<T> (no qualifier type args)
type Tree<T> {
    case Leaf(v: T);
    case _;
}
type Branch<T> extends Tree<T> {
    case Node(v: T);
}
def main() -> int {
	var t: Branch<int> = Branch.Node(30);
	match (t) {
	    Node(v) => return v;
	}
}
