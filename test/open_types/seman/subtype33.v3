//@seman = TypeError @ 9:12
// A method defined only in case _ is not accessible on a named case of the same variant
type A {
    case X;
    case _ {
        def kind() -> int { return 2; }
    }
}
def foo(x: A.X) -> int {
    return x.kind();
}
