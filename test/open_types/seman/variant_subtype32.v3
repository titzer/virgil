//@seman = TypeError @ 9:12
// A method defined only in case _ is not part of the root variant's interface
type A {
    case X;
    case _ {
        def kind() -> int { return 2; }
    }
}
def foo(a: A) -> int {
    return a.kind();
}
