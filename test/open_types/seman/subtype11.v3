//@seman
// case _ method without body (abstract); subtype provides the body
type A {
    case _ {
        def greet() -> int;
    }
}
type B extends A {
    case X;
    def greet() -> int { return 1; }
}
