//@seman
// Three-level chain: A.B.C can override method from A transitively
type A {
    case _;
    def greet() -> int;
}
type B extends A {
    case _;
}
type C extends B {
    case X;
    def greet() -> int;
}
