//@seman
// case _ method with body; subtype may override it
type A {
    case _ {
        def greet() -> int { return 0; }
    }
}
type B extends A {
    case X;
    def greet() -> int { return 1; }
}
