//@execute 0=32; 1=33; 2=1; 3=1
type Base {
	def m() -> int;

	case _;
}

type One<T>(x: T) extends Base {
	def m() => 1;
}
type Two(x: int, y: int) extends Base {
	def m() => x + y;
}

def bs = [Two(10, 22), Two(30, 3), One<int>(20), One<bool>(false)];

def main(a: int) => bs[a].m();
