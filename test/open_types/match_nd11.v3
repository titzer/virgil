//@execute 0=1; 1=0
type A { case _; }
type A.B { case P; case Q; }
def takesB(b: A.B) -> int {
    match (b) { P => return 1; Q => return 0; }
}
def main(arg: int) -> int {
    var a: A;
    if (arg == 0) a = A.B.P;
    else a = A.B.Q;
    if (A.B.?(a)) return takesB(A.B.!(a));
    return -1;
}
