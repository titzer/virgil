//@execute 0=1; 1=0
type A { case _; }
type B extends A { case P; case Q; }
def takesB(b: B) -> int {
    match (b) { P => return 1; Q => return 0; }
}
def main(arg: int) -> int {
    var a: A;
    if (arg == 0) a = B.P;
    else a = B.Q;
    if (B.?(a)) return takesB(B.!(a));
    return -1;
}
