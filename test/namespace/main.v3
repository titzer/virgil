def main() -> int {
    var util = Util.new(1);
    var duplicate = DuplicateName.new(1, 2, "test");
    return 0;
}

private class DuplicateName {
    new(param1: int, param2: int, param3: string) { }
}