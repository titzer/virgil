//@execute 0=0; 5=10

def main(l:int) -> int {

	var af64 = Array<double>.new(l);
	for (i < l) {
		af64[i] = double.!(i);
	}
	var sf64 = 0.0d;
	for (i < l) {
		sf64 += af64[i];
	}
	return int.!(sf64);
}
