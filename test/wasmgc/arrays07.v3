//@execute 0=0; 5=10

def main(l:int) -> int {

	var af32 = Array<float>.new(l);
	for (i < l) {
		af32[i] = float.!(i);
	}
	var sf32 = 0.0f;
	for (i < l) {
		sf32 += af32[i];
	}
	return int.!(sf32);
}
