//@execute 0=true; 1=false

// A test of simple type recursion

class A {
	var v: int;
	var a: A;
	new(v, a) { }
}

def main(args: Array<string>) -> int {
	var a: A;
	for (i < args.length) {
		a = A.new(i, a);
	}
	var j = 0;
	while (a != null) {
		j += a.v;
		a = a.a;
	}
	return j;
}
