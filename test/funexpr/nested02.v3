//@execute 0=4; 1=5; 2=6
var f: int;
def main(a: int) -> int {
	f = a;
	var m = fun (a: int, b: int, c: int, d: int, e: int, f: int) => a + b + c + d + e + f;
	return m(f, f = 0, f, f = 1, f, f = 2);
}

