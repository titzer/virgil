//@execute 0=42; 1=42
// Lambdas in inferred fields of two different compounds may legitimately share a name;
// they must still be kept apart.
def main(a: int) => if(a == 0, X.f(41), Y.f(40));
component X {
	def f = fun (x: int) => x + 1;
}
component Y {
	def f = fun (x: int) => x + 2;
}
