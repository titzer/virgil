//@seman
def sort<T>(a: Array<T>, f: (T, T) -> bool) -> Array<T> {
	return a;
}
def foo<T>() {
	var x = Array<T>.new(1);
	var y = sort(x, fun (a: T, b: T) -> bool {
		var f = fun => a;
		return a == b;
	});
}
