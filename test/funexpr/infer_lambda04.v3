//@execute 0=0; 1=5; 2=12; 7=77
// Nested lambdas inside inferred field initializers: inner lambdas collide too.
def main(a: int) => Accs.F(a)(a) + Accs.G(a)(a);
component Accs {
	def F = fun (x: int) => (fun (y: int) => x + y);
	def G = fun (x: int) => (fun (y: int) => x * y + x + y);
}
