//@execute 33=71; 44=93; -44=-83
def main(a: int) -> int {
	return sum([fun => (a - 11), fun => (a + 11), fun => 5]);
}
def sum(fs: Array<void -> int>) -> int {
	var s = 0;
	for (f in fs) s += f();
	return sum;
}
