//@execute 0=13; 1=14
class C { def n() => 13; }
class D extends C { def n() => 14; }
def fc() => C.new();
def fd() => D.new();

def adapter(f: () -> D, v: ()) -> C {
	return f();
}

def main(i: int) -> int {
	var arrf: Array<() -> C> = [fc, adapter(fd, _)];
	return arrf[i]().n();
}
