//@execute 0=0; 1=3; 4=12; -6=-18
// Inferred fields with lambdas in a generic class.
def main(a: int) -> int {
	var b = Box<int>.new(a);
	var t = b.dup(b.id(a));
	return t.0 + t.1 + b.id(a);
}
class Box<T>(v: T) {
	def id = fun (x: T) => x;
	def dup = fun (x: T) => (x, x);
}
