//@execute = true
component write03 {
	def main() -> bool {
		var str = "The quick brown fox jumped over the lazy dog.";
		var buf = Pointer.atContents(str);
		var r = Darwin.write(101, buf, str.length);
		return r < 0;
	}
}
