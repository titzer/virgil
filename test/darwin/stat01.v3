//@execute = 46
component stat01 {
	def statbuf: Array<int> = Array.new(32);
	def main() -> int {
		var path = Pointer.atContents("test.txt\x00");
		if (Darwin.stat(path, Pointer.atContents(statbuf)) != 0) return -1;
		return statbuf(12);
	}
}
