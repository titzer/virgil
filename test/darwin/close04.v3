//@execute = true
component close04 {
	def main() -> bool {
		var path = Pointer.atContents("test.txt\x00");
		var fd = Darwin.open(path, 0, 0);
		return Darwin.close(fd) == 0 && Darwin.close(fd) < 0;
	}
}
