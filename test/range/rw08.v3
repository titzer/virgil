//@execute (10,3,1)=4; (10,5,4)=7

def main(len: int, count: int, offset: int) -> int {
	var a = Array<u8>.new(len);
	for (i < len) a[i] = u8.!(i);
	var r = a[3 ..+ count];
	return int.!(r[offset]);
}
