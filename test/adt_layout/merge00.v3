//@execute =33
type A00 #unboxed {
	case X(a: u64);
	case Y(b: u32);
}

def f(a: A00) -> u32 {
	match (a) {
		X(a) => return u32.!(a);
		Y(b) => return b;
	}
}

def main() -> u32 {
	var a00_x = A00.X(22ul);
	var a00_y = A00.Y(11);

	return f(a00_x) + f(a00_y);
}