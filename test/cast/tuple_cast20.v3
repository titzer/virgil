//@execute 0=0; 1=1; 2=2; 3=3; 4=4
def KB: byte = 44;
def KI: int = 45;
def KL: long = -46;
def KZ: bool = true;

def f<A>(var a: A) -> A {
	var t = (KI, KB);
	if (A.?(t)) a = A.!(t);
	return a;
}
def testBase<A>() {
	f(A.default);
}
def test<A, B>() {
	var x: (A, A, B);
	f<(A, A, B)>(x);
}
def main(a: int) -> int {
	test<int, byte>();
	return a;
}