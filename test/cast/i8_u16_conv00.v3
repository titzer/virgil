//@execute 0=0; 127=127; -1=!TypeCheckException; -128=!TypeCheckException
// i8 -> u16: checked cast throws for any negative i8, since u16 cannot
// represent negative values; non-negative values pass through unchanged.
def main(a: int) -> int {
	var x = i8.!(a);
	return int.view(u16.!(x));
}
