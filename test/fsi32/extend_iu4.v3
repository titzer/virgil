//@execute 0=-9; 17=8; -100=-109
def main(a: int) -> int {
	for (i = a - 5; i < a + 5; i++) {
		var x = i3.view(i);
		var y = u4.view(x);
		if (int.view(y) < 0) return -1;
		if ((x & 0xF) != (y & 0xF)) return -2;
	}
	return a - 9;
}
