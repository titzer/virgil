//@execute 0=0; 1=0; -1=0; 77777=0; -99999=0
// Shifting a 32-bit int left by exactly its bit width yields 0 (mirrors the
// i64 oob case in opt_shl_imm_i64_oob.v3, which shifts by 64).
def shl_oob(a: int) -> int { return a << 32; }
def main(a: int) -> int {
	return shl_oob(a);
}
