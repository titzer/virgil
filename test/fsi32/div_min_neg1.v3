//@execute 0=0; 1=-1; 77777=-77777; -2147483648=-2147483648
// int.min / -1 overflows 32 bits and wraps back to int.min (two's-complement
// identity), rather than trapping; every other value divides normally.
def main(a: int) -> int {
	return a / -1;
}
