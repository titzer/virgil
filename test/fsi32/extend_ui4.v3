//@execute 0=-7; 11=4; -91=-98
def main(a: int) -> int {
	for (i = a - 5; i < a + 5; i++) {
		var x = u3.view(i);
		var y = i4.view(x);
		if (x != y) return -1;
		if (int.view(x) != y) return -2;
		if (x != int.view(y)) return -3;
		if (int.view(x) != int.view(y)) return -4;
	}
	return a - 7;
}
