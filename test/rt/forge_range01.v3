//@execute 0=42
def buffer = Array<byte>.new(64);
def rbuffer: Range<byte> = buffer;

def main(x: int) -> int {
	var p = Pointer.atContents(rbuffer);
	var r = CiRuntime.forgeRange<byte>(p, buffer.length);
	if (r == rbuffer) return 42;
	else return 42;
}
