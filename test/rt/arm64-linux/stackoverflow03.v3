// Bounded deep recursion must NOT be misdetected as a stack overflow.
def main(a: Array<string>) -> int {
	System.puts(">1\n");
	System.puti(sum(20000));
	System.puts("\n");
	return 0;
}
def sum(n: int) -> int {
	if (n <= 0) return 0;
	return n + sum(n - 1);
}
