// Enforces the API for CiCompiler.
def reserveCode = CiCompiler.reserveCodeRegion;
def reserveData = CiCompiler.reserveDataRegion;
def symbol = CiCompiler.addFunctionSymbol;
def patch32 = CiCompiler.patchAddressAbs32<string>;
def patch64 = CiCompiler.patchAddressAbs64<string>;

def main() -> int {
	if (reserveCode == null) return 1;
	if (reserveData == null) return 2;
	if (symbol == null) return 3;
	if (patch32 == null) return 4;
	if (patch64 == null) return 5;
	return 0;
}
