def main() -> int {
	var f = use(CiRuntime.forgeClosure<void, int, int>(Pointer.NULL, ()));
	System.puts("##+empty\n");
	System.puts("##-ok\n");
	return 0;
}
def use<T>(x: T) {
	var f = CiRuntime.callerSp(); // prevent inlining
}