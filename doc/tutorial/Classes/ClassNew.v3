class ClassNew {
	var a: int;
	var b: int;
}
component ClassNewDemo {
	// create a new object of type ClassNew by calling .new()
	var x = ClassNew.new();
	// create a second instance of the same class
	var y = ClassNew.new();
}