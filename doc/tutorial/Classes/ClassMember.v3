class ClassMember {
	var a: int = 3;
	def m(x: int) -> int {
		return x + a;
	}
}
def main() {
	var o = ClassMember.new();
	var y = o.a;     // field read
	o.a = 4;         // field write
	var z = o.m(10); // method call
	System.puti(z);
	System.puts("\n");
}
