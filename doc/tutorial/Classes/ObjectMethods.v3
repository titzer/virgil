class OmObject {
	def f: int;
	new(f) { }
	def add(a: int) -> int {
		return a + f;
	}
}
component ObjectMethods {
	var o = OmObject.new(3);
	// "obj.method" can be used as a first class function
	var m: int -> int = o.add;
	// the function is bound to the object from which it came
	var x = m(2);
}
