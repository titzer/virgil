def main(a: Array<string>) -> int {
	var x: int;
	// match an integer value
	match (a.length) {
		0: x = 9;
		// cases do not fall through
		1: x = 5;
		// cases do not fall through
		2: x = 7;
	}
	return x;
}
