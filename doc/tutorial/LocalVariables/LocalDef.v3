def main() {
	// 'def' introduces a new, immutable local variable
	def x = 0;             // with initializer
	def y: int = 0;        // with type and initializer
	def w: int;            // with type, initialized to default
	def t = true, u = ' '; // multiple declarations
}
