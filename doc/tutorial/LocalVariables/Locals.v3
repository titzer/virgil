def main() {
	// 'var' introduces a new, mutable local variable
	var x = 0;             // with initializer
	var y: int = 0;        // with type and initializer
	var w: int;            // with type, initialized to default
	var t = true, u = ' '; // multiple declarations
}
