// Parse the first argument as a positive decimal and return it. No error checking.
def main(a: Array<string>) -> int {
	if (a.length == 0) return 0;
	var arg = a[0], val = 0;
	for (i < arg.length) {
		var d = arg[i] - int.!('0');
		val = val * 10 + d;
	}
	return val;
}
