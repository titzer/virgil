// Entry point for wasm-gc target (main takes no args).
// Each run tests one case; set TEST and ITERS before compiling,
// or use the run-all.bash script which compiles per-test.

def main() -> int {
	return Bench.runTest(TEST, ITERS);
}

def TEST = 0;
def ITERS = 10000000;
