#!/bin/bash
DEV=$(dirname ${BASH_SOURCE[0]})
BIN=$(cd $DEV/.. && pwd)
SRC=$(cd $BIN/../aeneas/src && pwd)
LIB=$(cd $BIN/../lib/ && pwd)
$BIN/v3c -fp -run $SRC/*/*.v3 $LIB/util/*.v3 $LIB/asm/arm/*.v3 $LIB/asm/x86/*.v3 -a "$@"
